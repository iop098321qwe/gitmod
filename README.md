# Gitmod

Git convenience aliases for quick status and push workflows.
Keeps common commands short for everyday use.

## Functions
- None.

## Aliases
- `gpfom`: Run `git push --follow-tags origin main`.
- `gs`: Run `git status`.
